# Test: Simple Process Flow

Create a Mermaid flowchart for this process:

1. User submits registration form
2. System validates email format
3. If valid, create account and send confirmation email
4. If invalid, show error message and return to form

Requirements:
- Use left-to-right layout
- Include decision diamond for validation check
- Show both success and error paths
